\RequirePackage[greek, ngerman, main=english]{babel}
\RequirePackage[utf8]{inputenc}           % codierung damit man Umlaute direkt schreiben kann
\RequirePackage[T1]{fontenc}              % auch was mit codierung zu tun
\RequirePackage{lmodern}                  % Schriftart LatinModern Vektorisiert für schöneres PDF
\RequirePackage[onehalfspacing]{setspace} % Korrekter Zeilenabstand nach IPMB Vorgabe
\RequirePackage[version=4]{mhchem}      % Chemische Formeln
\RequirePackage{booktabs}               % für schöne Linien in Tabellen
\RequirePackage{siunitx}                % Si Einheiten
\RequirePackage{longtable}              % Tabelle mit Seitenumbrüchen
\RequirePackage{makecell}               % Allow multi column cells in tables.
\RequirePackage{multirow}               % Mehrere Reihen in einer Tabelle zusammenfassen
\RequirePackage{rotate}                 % Rotate table cell content
\RequirePackage{acronym}                % Abkürzungen
\RequirePackage{lscape}                 % If need landscape pages
\RequirePackage{url}
    \urlstyle{same}

%%% Bibliographie mit biber %%%
\RequirePackage[backend=biber, style=authoryear, giveninits=true, maxbibnames=99, uniquename=init, maxcitenames=2, hyperref=true]{biblatex}    % Benutze Biber/BibLaTeX zum Zitieren
\RequirePackage{xpatch}
\RequirePackage{csquotes}
\addbibresource{bib/essay.bib}          % Pfad zur BibTeX Datei aus Citavi

% HIER EINS VON BEIDEN AUSWÄHLEN
%\input{bib/cellstyle}                  % Cell Stil
\input{bib/cellstyle_it}                % Cell Stil mit kursivem et. al.

\RequirePackage{subfigure}              % für mehrere Unterabbildungen in einer Abbildung
\RequirePackage{graphicx}               % für Größenänderungen für Grafiken
\RequirePackage{rotating}               % für Tabellen im Querformat
\RequirePackage{amsmath}                % für Mathe
%\RequirePackage{float}                 % um Bilder an Ort und Stelle zu fixieren
\RequirePackage[left=2.50cm, right=2.50cm, top=2.50cm, bottom=2.50cm]{geometry}    % Seitenränder nach Vorgabe von IPMB
\RequirePackage{lettrine}


%\RequirePackage{blindtext}             % Blindtext zum Testen
\RequirePackage{lipsum}                 % Better blindtext package

\RequirePackage[hidelinks]{hyperref}    % Hyperlinks [hidelinks] um linkboxen zu deaktivieren
\hypersetup{                            % Better coloring of links, if not  wanted comment out
    colorlinks = true,
    allcolors = [rgb]{0.122, 0.133, 0.443},
}


\RequirePackage{scrhack}                % Anpassung von anderen Paketen an KOMA-Script zur besseren Kompatibilität

% Additional referencing commands, to make figure/table/eqution referencing the same everywhere.
% For free insertion:
\newcommand*{\Tref}[1]{Table~\ref{#1}}
\newcommand*{\Fref}[1]{Figure~\ref{#1}}
\newcommand*{\Eref}[1]{Equation~\ref{#1}}
% For reffing in Text:
\newcommand*{\sTref}[1]{(see \Tref{#1})}
\newcommand*{\sFref}[1]{(see \Fref{#1})}
\newcommand*{\sEref}[1]{(see \Eref{#1})}

\emergencystretch=50pt                  % Vergrößert Wortzwischenraum notfalls, wenn sonst die Zeile überläuft


\clubpenalty = 10000                    % Verhindern von Schusterjungen
\widowpenalty = 10000                   % Hurenkindern
\displaywidowpenalty = 10000            % und shit vor Formeln


\setkomafont{title}{\sffamily}                    % Festsetzen der Überschriftart auf Sans Serif
\setkomafont{disposition}{\usekomafont{title}}    % Festsetzen der weiteren Unterüberschriften auf Sans Serif
\setkomafont{author}{\usekomafont{title}}
\setkomafont{date}{\usekomafont{title}}

